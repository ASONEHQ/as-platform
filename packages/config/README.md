# @asone/config

Typed, immutable environment configuration for AS ONE applications. Application entry points validate `process.env` once and pass configuration inward; business modules must not read environment variables directly.
